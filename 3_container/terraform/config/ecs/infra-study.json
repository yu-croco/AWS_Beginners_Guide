[
  {
    "cpu": 1024,
    "environment": [],
    "essential": true,
    "image": "set your ECR endpoint",
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "/ecs/infra-study",
        "awslogs-region": "ap-northeast-1",
        "awslogs-stream-prefix": "ecs"
      }
    },
    "mountPoints": [],
    "name": "infra-study",
    "portMappings": [
      {
        "containerPort": 80,
        "hostPort": 80,
        "protocol": "tcp"
      }
    ],
    "secrets": [],
    "volumesFrom": []
  }
]
